apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: wac-hospital
commonLabels:
  app.kubernetes.io/component: dex

resources:
  - deployment.yaml
  - service.yaml
  - http-route.yaml
  - rbac.yaml

configMapGenerator:
  - name: dex-config
    files:
      - config.yaml